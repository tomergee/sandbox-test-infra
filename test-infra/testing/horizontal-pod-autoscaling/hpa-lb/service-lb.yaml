apiVersion: v1
kind: Service
metadata:
  name: {{.Name}}
  annotations:
    networking.gke.io/max-rate-per-endpoint: "10"
spec:
  ports:
  - port: 8080
    targetPort: 8080
    name: http
  selector:
    app: app-{{.Index}}
  type: ClusterIP
