apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{.Name}}
  labels:
    group: upsize-deployment
spec:
  replicas: {{.Replicas}}
  selector:
    matchLabels:
      group: upsize-test
  template:
    metadata:
      labels:
        group: upsize-test
    spec:
      containers:
        - name: pause
          image: k8s.gcr.io/pause:3.1
          resources:
            requests:
              cpu: "1"
              memory: "4Gi"