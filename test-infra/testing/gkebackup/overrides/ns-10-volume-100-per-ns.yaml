BACKUP_TIMEOUT: "4h"
RESTORE_TIMEOUT: "6h"

NAMESPACES: 10
SMALL_CONFIG_MAP_COUNT: 0
SMALL_SECRET_COUNT: 0

JOBS_PER_NAMESPACE: 0
DAEMON_SETS_PER_NAMESPACE: 0
DEPLOYMENTS_PER_NAMESPACE: 0

HAS_VOLUMES: true

# Each StatefulSet replica has one PVC, total 150 PVCs in one namespace.

STATEFUL_SETS_PER_NAMESPACE: 10
STATEFUL_SET_SIZE: 10
